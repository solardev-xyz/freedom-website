{
  "name": "freedom-website",
  "version": "1.0.0",
  "description": "Source and build tooling for the freedom.baby landing page",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "npm run build && npx --yes serve dist"
  },
  "dependencies": {
    "marked": "^18.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
